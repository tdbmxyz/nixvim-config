{
  diagnostic.settings = {
    underline = true;
    update_in_insert = false;
    # Virtual text everywhere except the current line,
    # full virtual lines on the current line only.
    virtual_text.current_line = false;
    virtual_lines.current_line = true;
  };
}
